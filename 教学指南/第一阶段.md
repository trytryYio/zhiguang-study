# 第一阶段：基础环境搭建 — 手把手任务流程

> 本文档严格对照 `源代码/zhiguang_be/` 和 `源代码/zhiguang_fe/` 的实际源码编写。
> 每个文件包含：创建路径、完整代码、注释说明后续阶段会如何改动。
>
> **目标**：后端能启动 Spring Boot 应用，前端能看到带主题色和侧边栏的空壳应用。

---

# 后端部分

## 任务 1：创建项目结构

### 操作步骤

```bash
# 在 IDEA 中创建 Spring Boot 项目，或手动创建目录结构：
源代码/zhiguang_be/
├── pom.xml
├── db/
│   └── schema.sql
└── src/
    └── main/
        ├── java/com/tongji/
        │   ├── ZhiGuangApplication.java
        │   ├── config/
        │   │   ├── ThreadPoolConfig.java
        │   │   └── RedissonConfig.java
        │   ├── cache/
        │   │   └── config/
        │   │       ├── CacheConfig.java
        │   │       └── CacheProperties.java
        │   └── auth/
        │       └── config/
        │           └── SecurityConfig.java
        └── resources/
            ├── application.yml
            └── mapper/
```

> **包名**：`com.tongji`（后续所有模块都在这个包下）
>
> **后续改动提示**：
> - 第二阶段会新增 `auth/service/`, `auth/token/`, `auth/api/`, `user/` 等包
> - 第三阶段会新增 `profile/`, `knowpost/`, `storage/` 等包
> - 第四阶段会新增 `counter/`, `relation/` 等包
> - 第五阶段会新增 `llm/`, `search/` 等包

---

## 任务 2：编写 `pom.xml`

**路径**：`源代码/zhiguang_be/pom.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.4</version>
        <relativePath/>
    </parent>

    <groupId>org.example</groupId>
    <artifactId>zhiguang</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>zhiguang</name>
    <description>ZhiGuang - knowledge community platform</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases><enabled>false</enabled></releases>
        </repository>
        <repository>
            <id>central-portal-snapshots</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <!-- Spring AI BOM 统一版本 -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>1.0.3</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ===== 第一阶段必需依赖 ===== -->

        <!-- Web 基础 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- 参数校验 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- JDBC + MyBatis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>3.0.3</version>
        </dependency>

        <!-- MySQL 驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.5.0</version>
        </dependency>

        <!-- Spring Security（第二阶段 JWT 认证基础） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- Redis + Redisson（缓存、验证码存储） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson</artifactId>
            <version>3.52.0</version>
        </dependency>

        <!-- Caffeine 本地缓存 -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>3.1.8</version>
        </dependency>

        <!-- Actuator 健康检查 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Lombok 减少样板代码 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.30</version>
            <scope>provided</scope>
        </dependency>

        <!-- 邮件发送（第二阶段验证码用） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <!-- 线程池（第一阶段 ThreadPoolConfig 用到） -->
        <!-- 已包含在 spring-boot-starter-web 中 -->

        <!-- ===== 后续阶段依赖（第一阶段先写上，避免后续频繁改 pom） ===== -->

        <!-- 第五阶段：Elasticsearch 搜索 -->
        <dependency>
            <groupId>co.elastic.clients</groupId>
            <artifactId>elasticsearch-java</artifactId>
            <version>9.2.1</version>
        </dependency>
        <dependency>
            <groupId>org.elasticsearch.client</groupId>
            <artifactId>elasticsearch-rest-client</artifactId>
            <version>9.2.1</version>
        </dependency>

        <!-- 第五阶段：Spring AI 向量存储 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-vector-store-elasticsearch</artifactId>
        </dependency>

        <!-- 第五阶段：OpenAI / DeepSeek 模型 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-deepseek</artifactId>
        </dependency>

        <!-- 第五阶段：Kafka 异步消息 -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <!-- 第三阶段：阿里云 OSS 对象存储 -->
        <dependency>
            <groupId>com.aliyun.oss</groupId>
            <artifactId>aliyun-sdk-oss</artifactId>
            <version>3.17.3</version>
        </dependency>

        <!-- 第五阶段：Canal 数据同步 -->
        <dependency>
            <groupId>com.alibaba.otter</groupId>
            <artifactId>canal.client</artifactId>
            <version>1.1.8</version>
        </dependency>
        <dependency>
            <groupId>com.alibaba.otter</groupId>
            <artifactId>canal.common</artifactId>
            <version>1.1.8</version>
        </dependency>
        <dependency>
            <groupId>com.alibaba.otter</groupId>
            <artifactId>canal.protocol</artifactId>
            <version>1.1.8</version>
        </dependency>

        <!-- 测试依赖 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.tongji.ZhiGuangApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
```

> **后续改动**：
> - `pom.xml` 第一阶段一次性写全，后续阶段一般不需要改
> - 如果后续引入新依赖（如新 SDK），在此文件的 `<dependencies>` 中追加

---

## 任务 3：创建数据库 schema

**路径**：`源代码/zhiguang_be/db/schema.sql`

```sql
-- MySQL 8.0 schema for ZhiGuang

-- 用户表
CREATE TABLE IF NOT EXISTS users (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    phone VARCHAR(32) NULL,
    email VARCHAR(128) NULL,
    password_hash VARCHAR(128) NULL,
    nickname VARCHAR(64) NOT NULL,
    avatar TEXT NULL,
    bio VARCHAR(512) NULL,
    zg_id VARCHAR(64) NULL,
    gender VARCHAR(16) NULL,
    birthday DATE NULL,
    school VARCHAR(128) NULL,
    tags_json JSON NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_users_phone (phone),
    UNIQUE KEY uk_users_email (email),
    UNIQUE KEY uk_users_zg_id (zg_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 登录日志表（第二阶段认证用）
CREATE TABLE IF NOT EXISTS login_logs (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    user_id BIGINT UNSIGNED NULL,
    identifier VARCHAR(128) NOT NULL,
    channel VARCHAR(32) NOT NULL,
    ip VARCHAR(45) NULL,
    user_agent VARCHAR(512) NULL,
    status VARCHAR(16) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY ix_login_logs_user_created_at (user_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 知文（KnowPost）主表（第三阶段内容发布用）
CREATE TABLE IF NOT EXISTS know_posts (
    id BIGINT UNSIGNED NOT NULL,
    tag_id BIGINT UNSIGNED NULL COMMENT '主分类/内容分类ID',
    tags JSON NULL COMMENT '标签名数组，例如 ["java","编程"]',
    title VARCHAR(256) NULL,
    description VARCHAR(50) NULL COMMENT '摘要/描述，最多50字',
    content_url TEXT NULL COMMENT '正文存储于OSS的访问URL',
    content_object_key VARCHAR(512) NULL COMMENT 'OSS对象Key',
    content_etag VARCHAR(128) NULL COMMENT 'OSS ETag（用于校验）',
    content_size BIGINT UNSIGNED NULL COMMENT '正文字节大小',
    content_sha256 CHAR(64) NULL COMMENT '正文SHA-256哈希（hex）',
    creator_id BIGINT UNSIGNED NOT NULL,
    is_top TINYINT(1) NOT NULL DEFAULT 0,
    type VARCHAR(32) NOT NULL DEFAULT 'image_text',
    visible VARCHAR(32) NOT NULL DEFAULT 'public',
    img_urls JSON NULL COMMENT '图片URL数组',
    video_url TEXT NULL COMMENT '视频URL（一期不使用）',
    status VARCHAR(16) NOT NULL DEFAULT 'draft',
    create_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    publish_time TIMESTAMP NULL DEFAULT NULL,
    PRIMARY KEY (id),
    KEY ix_know_posts_creator_ct (creator_id, create_time),
    KEY ix_know_posts_status_ct (status, create_time),
    KEY ix_know_posts_tag_ct (tag_id, create_time),
    KEY ix_know_posts_top_ct (is_top, create_time),
    KEY ix_know_posts_creator_status_pub (creator_id, status, publish_time),
    CONSTRAINT fk_know_posts_creator FOREIGN KEY (creator_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Outbox 表（第四阶段事件驱动用）
CREATE TABLE IF NOT EXISTS outbox (
    id BIGINT UNSIGNED NOT NULL,
    aggregate_type VARCHAR(64) NOT NULL,
    aggregate_id BIGINT UNSIGNED NULL,
    type VARCHAR(64) NOT NULL,
    payload JSON NOT NULL,
    created_at TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (id),
    KEY ix_outbox_agg (aggregate_type, aggregate_id),
    KEY ix_outbox_ct (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 关注表（第四阶段社交关系用）
CREATE TABLE IF NOT EXISTS following (
    id BIGINT UNSIGNED NOT NULL,
    from_user_id BIGINT UNSIGNED NOT NULL,
    to_user_id BIGINT UNSIGNED NOT NULL,
    rel_status TINYINT NOT NULL DEFAULT 1,
    created_at DATETIME(3) NOT NULL,
    updated_at DATETIME(3) NOT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_from_to (from_user_id, to_user_id),
    KEY idx_from_created (from_user_id, created_at, to_user_id, rel_status),
    KEY idx_to (to_user_id, from_user_id, rel_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 粉丝表（第四阶段社交关系用）
CREATE TABLE IF NOT EXISTS follower (
    id BIGINT UNSIGNED NOT NULL,
    to_user_id BIGINT UNSIGNED NOT NULL,
    from_user_id BIGINT UNSIGNED NOT NULL,
    rel_status TINYINT NOT NULL DEFAULT 1,
    created_at DATETIME(3) NOT NULL,
    updated_at DATETIME(3) NOT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_to_from (to_user_id, from_user_id),
    KEY idx_to_created (to_user_id, created_at, from_user_id, rel_status),
    KEY idx_from (from_user_id, to_user_id, rel_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

> **后续改动**：
> - 第一阶段建好全部表结构，后续阶段**不需要改表**
> - 第二阶段主要操作 `users` 和 `login_logs`
> - 第三阶段主要操作 `know_posts`
> - 第四阶段主要操作 `following`、`follower`、`outbox`

---

## 任务 4：编写配置文件

**路径**：`源代码/zhiguang_be/src/main/resources/application.yml`

```yaml
server:
  port: 8080

spring:
  # ===== 数据库配置 =====
  datasource:
    url: jdbc:mysql://localhost:3306/zhiguang?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: your_password          # TODO: 改为你的 MySQL 密码
    driver-class-name: com.mysql.cj.jdbc.Driver

  # ===== Redis 配置 =====
  data:
    redis:
      host: localhost
      port: 6379
      password:                      # TODO: 如果 Redis 有密码，在此填写
      database: 0

  # ===== 邮件配置（第二阶段验证码用） =====
  mail:
    host: smtp.example.com           # TODO: 改为你的 SMTP 服务器
    port: 587
    username: your_email@example.com # TODO: 改为你的邮箱
    password: your_email_password    # TODO: 改为你的邮箱密码

  # ===== MyBatis 配置 =====
  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.tongji
    configuration:
      map-underscore-to-camel-case: true

  # ===== Spring AI OpenAI（第五阶段用，第一阶段可留空） =====
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-placeholder}
      base-url: https://api.openai.com
    deepseek:
      api-key: ${DEEPSEEK_API_KEY:sk-placeholder}
      base-url: https://api.deepseek.com

# ===== JWT 配置（第二阶段用） =====
jwt:
  access-token-expiration: 900       # 15分钟
  refresh-token-expiration: 604800   # 7天
  key-id: zhiguang-key
  public-key-location: classpath:keys/public.pem
  private-key-location: classpath:keys/private.pem

# ===== 缓存配置（第一阶段用） =====
cache:
  l2:
    public-cfg:
      ttl-seconds: 15
      max-size: 1000
    mine-cfg:
      ttl-seconds: 10
      max-size: 1000
    detail-cfg:
      ttl-seconds: 30
      max-size: 5000
  hotkey:
    window-seconds: 60
    segment-seconds: 10
    level-low: 50
    level-medium: 200
    level-high: 500
    extend-low-seconds: 20
    extend-medium-seconds: 60
    extend-high-seconds: 120

# ===== 计数系统配置（第四阶段用） =====
counter:
  rebuild:
    lock:
      watchdog-ms: 30000

# ===== Actuator 配置 =====
management:
  endpoints:
    web:
      exposure:
        include: health,info
```

> **后续改动**：
> - 第二阶段需要确保 `jwt.*` 配置正确并生成 RSA 密钥对
> - 第五阶段需要填写真实的 `OPENAI_API_KEY` 和 `DEEPSEEK_API_KEY`
> - 第三阶段可能需要增加 OSS 配置

---

## 任务 5：生成 RSA 密钥对（第二阶段 JWT 用）

```bash
# 在 src/main/resources/ 下创建 keys 目录
mkdir -p 源代码/zhiguang_be/src/main/resources/keys

# 生成私钥（2048位 RSA）
openssl genrsa -out 源代码/zhiguang_be/src/main/resources/keys/private.pem 2048

# 从私钥提取公钥
openssl rsa -in 源代码/zhiguang_be/src/main/resources/keys/private.pem -pubout -out 源代码/zhiguang_be/src/main/resources/keys/public.pem
```

> **后续改动**：
> - 密钥对只在第二阶段认证系统中使用，生成后**不再改动**
> - 生产环境需要妥善保管私钥，不可提交到 Git

---

## 任务 6：应用启动类

**路径**：`源代码/zhiguang_be/src/main/java/com/tongji/ZhiGuangApplication.java`

```java
package com.tongji;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ZhiGuangApplication {

    public static void main(String[] args) {
        SpringApplication.run(ZhiGuangApplication.class, args);
    }
}
```

> **后续改动**：
> - 这个文件**全程不变**，后续所有模块通过 `@ComponentScan` 自动扫描
> - 如果需要自定义 Mapper 扫描路径，可能在类上加 `@MapperScan("com.tongji.**.mapper")`

---

## 任务 7：线程池配置

**路径**：`源代码/zhiguang_be/src/main/java/com/tongji/config/ThreadPoolConfig.java`

```java
package com.tongji.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;

import java.util.concurrent.ThreadPoolExecutor;

/**
 * 线程池配置。
 *
 * <p>用于异步任务处理（如后续阶段的异步计数、异步邮件发送等）。</p>
 */
@Configuration
public class ThreadPoolConfig {

    @Bean(name = "taskExecutor")
    public ThreadPoolTaskExecutor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(10);              // 核心线程数
        executor.setMaxPoolSize(50);               // 最大线程数
        executor.setQueueCapacity(200);            // 队列容量
        executor.setKeepAliveSeconds(30);          // 空闲线程存活时间
        executor.setThreadNamePrefix("NoteExecutor-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setWaitForTasksToCompleteOnShutdown(true);
        executor.setAwaitTerminationSeconds(60);
        executor.initialize();
        return executor;
    }
}
```

> **后续改动**：
> - 第四阶段可能增加更多线程池 Bean（如 Kafka 消费者专用线程池）
> - 第二阶段发送邮件时会使用 `@Async` + 此线程池

---

## 任务 8：Redisson 配置

**路径**：`源代码/zhiguang_be/src/main/java/com/tongji/config/RedissonConfig.java`

```java
package com.tongji.config;

import org.redisson.Redisson;
import org.redisson.api.RedissonClient;
import org.redisson.config.Config;
import org.redisson.config.SingleServerConfig;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.boot.autoconfigure.data.redis.RedisProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * Redisson 客户端配置。
 *
 * <p>用于分布式锁（如第四阶段计数重建锁）。</p>
 */
@Configuration
public class RedissonConfig {

    @Value("${counter.rebuild.lock.watchdog-ms:30000}")
    private long lockWatchdogMs;

    @Bean
    public RedissonClient redissonClient(RedisProperties redisProperties) {
        Config config = new Config();
        config.setLockWatchdogTimeout(lockWatchdogMs);

        String address = "redis://" + redisProperties.getHost() + ":" + redisProperties.getPort();
        SingleServerConfig single = config.useSingleServer().setAddress(address);

        if (redisProperties.getPassword() != null && !redisProperties.getPassword().isEmpty()) {
            single.setPassword(redisProperties.getPassword());
        }

        single.setDatabase(redisProperties.getDatabase());
        return Redisson.create(config);
    }
}
```

> **后续改动**：
> - 如果后续改为 Redis 集群模式，需将 `useSingleServer()` 改为 `useClusterServers()`
> - 第四阶段计数重建锁依赖此 Bean

---

## 任务 9：Caffeine 缓存配置

**路径**：`源代码/zhiguang_be/src/main/java/com/tongji/cache/config/CacheConfig.java`

```java
package com.tongji.cache.config;

import com.github.benmanes.caffeine.cache.Cache;
import com.github.benmanes.caffeine.cache.Caffeine;
// 以下两个类型在第三阶段才会用到，但这里先声明，避免编译报错
import com.tongji.knowpost.api.dto.FeedPageResponse;       // 第三阶段创建
import com.tongji.knowpost.api.dto.KnowPostDetailResponse;  // 第三阶段创建

import java.time.Duration;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * Caffeine 本地缓存配置（L2 缓存）。
 *
 * <p>用于在应用进程内缓存分页结果和详情数据，降低数据库压力。</p>
 */
@Configuration
public class CacheConfig {

    /**
     * 公共信息流分页缓存。
     *
     * <p>TTL 和最大条目由 application.yml 中的 cache.l2.public-cfg 控制。</p>
     */
    @Bean("feedPublicCache")
    public Cache<String, FeedPageResponse> feedPublicCache(CacheProperties props) {
        return Caffeine.newBuilder()
                .maximumSize(props.getL2().getPublicCfg().getMaxSize())
                .expireAfterWrite(Duration.ofSeconds(props.getL2().getPublicCfg().getTtlSeconds()))
                .build();
    }

    /**
     * 个人信息流分页缓存。
     */
    @Bean("feedMineCache")
    public Cache<String, FeedPageResponse> feedMineCache(CacheProperties props) {
        return Caffeine.newBuilder()
                .maximumSize(props.getL2().getMineCfg().getMaxSize())
                .expireAfterWrite(Duration.ofSeconds(props.getL2().getMineCfg().getTtlSeconds()))
                .build();
    }

    /**
     * 知文详情本地缓存。
     */
    @Bean("knowPostDetailCache")
    public Cache<String, KnowPostDetailResponse> knowPostDetailCache(CacheProperties props) {
        return Caffeine.newBuilder()
                .maximumSize(props.getL2().getDetailCfg().getMaxSize())
                .expireAfterWrite(Duration.ofSeconds(props.getL2().getDetailCfg().getTtlSeconds()))
                .build();
    }
}
```

> **重要**：`FeedPageResponse` 和 `KnowPostDetailResponse` 是第三阶段才创建的 DTO。
> 第一阶段创建此配置时，**先注释掉这两个 import 和对应 Bean**，等第三阶段创建 DTO 后再放开。
> 或者你可以先创建空的 DTO 类让编译通过：

```java
// 第一阶段临时占位（第三阶段会被真实代码覆盖）
// 路径：源代码/zhiguang_be/src/main/java/com/tongji/knowpost/api/dto/FeedPageResponse.java
package com.tongji.knowpost.api.dto;
public class FeedPageResponse {}

// 路径：源代码/zhiguang_be/src/main/java/com/tongji/knowpost/api/dto/KnowPostDetailResponse.java
package com.tongji.knowpost.api.dto;
public class KnowPostDetailResponse {}
```

**路径**：`源代码/zhiguang_be/src/main/java/com/tongji/cache/config/CacheProperties.java`

```java
package com.tongji.cache.config;

import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.stereotype.Component;

/**
 * 缓存相关配置项，绑定 application.yml 中的 cache.*。
 */
@Component
@ConfigurationProperties(prefix = "cache")
@Data
public class CacheProperties {

    private L2 l2 = new L2();
    private Hotkey hotkey = new Hotkey();

    @Data
    public static class L2 {
        private PublicCfg publicCfg = new PublicCfg();
        private MineCfg mineCfg = new MineCfg();
        private DetailCfg detailCfg = new DetailCfg();
    }

    @Data
    public static class PublicCfg {
        private int ttlSeconds = 15;
        private long maxSize = 1000;
    }

    @Data
    public static class MineCfg {
        private int ttlSeconds = 10;
        private long maxSize = 1000;
    }

    @Data
    public static class DetailCfg {
        private int ttlSeconds = 30;
        private long maxSize = 5000;
    }

    @Data
    public static class Hotkey {
        private int windowSeconds = 60;
        private int segmentSeconds = 10;
        private int levelLow = 50;
        private int levelMedium = 200;
        private int levelHigh = 500;
        private int extendLowSeconds = 20;
        private int extendMediumSeconds = 60;
        private int extendHighSeconds = 120;
    }
}
```

> **后续改动**：
> - `CacheProperties` 第一阶段完成后基本**不再改动**
> - 第六阶段可能调整 TTL 和 maxSize 的值以优化性能

---

## 任务 10：Spring Security 配置

**路径**：`源代码/zhiguang_be/src/main/java/com/tongji/auth/config/SecurityConfig.java`

```java
package com.tongji.auth.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.Customizer;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.annotation.web.configurers.AbstractHttpConfigurer;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.CorsConfigurationSource;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;

import java.util.List;

/**
 * Spring Security 安全配置。
 *
 * <p>第一阶段先配置基础安全策略，第二阶段会加入 JWT 过滤器。</p>
 */
@Configuration
@EnableWebSecurity
@EnableMethodSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
                .csrf(AbstractHttpConfigurer::disable)
                .cors(Customizer.withDefaults())
                .sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
                .authorizeHttpRequests(auth -> auth
                        .requestMatchers("/actuator/health", "/actuator/info").permitAll()
                        // 公开内容：首页 Feed 不需要登录
                        .requestMatchers("/api/v1/knowposts/feed").permitAll()
                        // 知文详情（公开已发布内容）
                        .requestMatchers(org.springframework.http.HttpMethod.GET, "/api/v1/knowposts/detail/*").permitAll()
                        // RAG 问答允许匿名
                        .requestMatchers(org.springframework.http.HttpMethod.GET, "/api/v1/knowposts/*/qa/stream").permitAll()
                        // 认证接口公开
                        .requestMatchers(
                                "/api/v1/auth/send-code",
                                "/api/v1/auth/register",
                                "/api/v1/auth/login",
                                "/api/v1/auth/token/refresh",
                                "/api/v1/auth/logout",
                                "/api/v1/auth/password/reset"
                        ).permitAll()
                        .anyRequest().authenticated()
                )
                .oauth2ResourceServer(oauth -> oauth.jwt(Customizer.withDefaults()));
        return http.build();
    }

    @Bean
    public CorsConfigurationSource corsConfigurationSource() {
        CorsConfiguration configuration = new CorsConfiguration();
        configuration.setAllowedOrigins(List.of("*")); // TODO: 后续替换为产品白名单
        configuration.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
        configuration.setAllowedHeaders(List.of("Authorization", "Content-Type", "X-Requested-With"));
        configuration.setAllowCredentials(false);
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", configuration);
        return source;
    }
}
```

> **后续改动（第二阶段）**：
> - 第二阶段需要新增 `JwtAuthenticationFilter.java`，并在 `.oauth2ResourceServer()` 前插入 JWT 过滤器
> - `corsConfigurationSource()` 的 `*` 需改为前端域名白名单
> - 当前阶段先保持，确保编译通过即可

---

## 后端第一阶段完成检查

```bash
cd 源代码/zhiguang_be

# 编译检查（不运行）
mvn compile

# 如果看到 BUILD SUCCESS，说明依赖、配置、类都正确
# 然后启动应用：
mvn spring-boot:run

# 浏览器访问 http://localhost:8080/actuator/health
# 应返回：{"status":"UP"}
```

> **预期结果**：Spring Boot 启动成功，actuator 健康检查返回 `UP`。
> **注意事项**：
> - MySQL 必须先启动并创建 `zhiguang` 数据库
> - Redis 必须先启动
> - `application.yml` 中的数据库密码必须正确填写

---

# 前端部分

## 任务 1：创建 Vite 项目

```bash
# 在工作目录下执行
npm create vite@latest zhiguang_new -- --template react-ts

# 进入项目目录
cd zhiguang_new

# 安装 Vite 创建时自带的基础依赖（react, react-dom, typescript, vite 等）
npm install

# 安装路由
npm install react-router-dom

# 安装开发依赖（别名解析、类型声明、PostCSS）
npm install -D @types/node @types/react @types/react-dom @vitejs/plugin-react autoprefixer postcss
```

> **后续改动**：
> - 第二阶段：无新依赖（使用原生 fetch）
> - 第五阶段：`npm install react-markdown remark-gfm clsx @coze/api`

---

## 任务 2：配置 `vite.config.ts`

> **文件作用**：Vite 构建工具的核心配置文件。定义插件（React 支持）、开发服务器端口、API 代理规则、路径别名（`@/` 指向 `src/`）。
> 类似后端的 `application.yml`，告诉 Vite "如何运行和构建项目"。

**路径**：`源代码/zhiguang_fe/vite.config.ts`

```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import { fileURLToPath, URL } from "node:url";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      "/api": {
        target: "http://localhost:8080",
        changeOrigin: true
      }
    }
  },
  resolve: {
    alias: {
      "@": fileURLToPath(new URL("./src", import.meta.url))
    }
  }
});
```

> **关键字段说明**：
> - `plugins: [react()]` — 启用 React 支持（JSX 编译、Fast Refresh 热更新）
> - `server.port: 5173` — 开发服务器端口（Vite 默认也是 5173）
> - `server.proxy["/api"]` — 将 `/api/xxx` 请求转发到 `http://localhost:8080/api/xxx`，解决跨域
> - `resolve.alias["@"]` — 让代码中可以用 `import Xxx from "@/components/Xxx"` 代替冗长的相对路径
>
> **后续改动**：
> - 第一阶段完成后基本**不再改动**
> - 如果后端端口改变，更新 `target` 端口
> - 生产环境会使用 Nginx 反向代理，不再需要 Vite proxy

---

## 任务 3：配置 `tsconfig.json`

> **文件作用**：TypeScript 编译器的配置文件。告诉 TypeScript "如何检查你的代码"、"用什么模块系统"、"是否允许 JSX"等。
> 类似后端的 `pom.xml` 中 `<properties>` 部分 + 编译器插件配置。
> 这里还配置了**路径别名** `@/*` → `src/*`，和 `vite.config.ts` 中的 alias 配合使用。

**路径**：`源代码/zhiguang_fe/tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "lib": [
      "DOM",
      "DOM.Iterable",
      "ESNext"
    ],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": false,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "types": [
      "vite/client"
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "src/*"
      ]
    }
  },
  "include": [
    "src"
  ],
  "references": [
    {
      "path": "./tsconfig.node.json"
    }
  ]
}
```

> **关键字段说明**：
> - `target: "ESNext"` — 编译目标为最新 JavaScript 语法
> - `lib: ["DOM", "DOM.Iterable", "ESNext"]` — 包含浏览器 DOM API 和最新 ES 类型的类型声明
> - `strict: true` — 开启所有严格类型检查（推荐，尽早发现 bug）
> - `jsx: "react-jsx"` — 使用 React 17+ 的新 JSX 转换（不需要手动 `import React`）
> - `moduleResolution: "Node"` — 用 Node.js 风格的模块解析（找 `node_modules`）
> - `noEmit: true` — 不输出编译后的 JS 文件（Vite 负责编译，TS 只做类型检查）
> - `types: ["vite/client"]` — 引入 `import.meta.env` 等 Vite 全局变量的类型
> - `baseUrl` + `paths` — 路径别名：`@/xxx` 解析为 `src/xxx`
> - `references` — 项目引用，指向 `tsconfig.node.json`（分离编译范围）
>
> **后续改动**：**不再改动**

---

## 任务 4：配置 `tsconfig.node.json`

> **文件作用**：这是 `tsconfig.json` 的"兄弟"文件，专门用来编译 `vite.config.ts` 等 Node.js 运行时的配置文件。
> 因为 `vite.config.ts` 运行在 Node.js 环境（不是浏览器），所以需要单独的 TS 配置。
> 通过 `references` 字段把两个编译范围隔离开，避免浏览器 API 和 Node.js API 混在一起报错。

**路径**：`源代码/zhiguang_fe/tsconfig.node.json`

```json
{
  "compilerOptions": {
    "composite": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "allowSyntheticDefaultImports": true
  },
  "include": [
    "vite.config.ts"
  ]
}
```

> **关键字段说明**：
> - `composite: true` — 声明这是一个"被引用"的项目，配合 `references` 使用
> - `include: ["vite.config.ts"]` — 只编译 vite 配置文件
>
> **后续改动**：**不再改动**

---

## 任务 5：创建 `index.html`

> **文件作用**：HTML 入口页面。Vite 会把 `src/main.tsx` 编译后注入到这个文件中。
> 类似后端的 `index.html` 或模板引擎的根页面，浏览器首先加载这个文件。

**路径**：`源代码/zhiguang_fe/index.html`

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>知光 - 让知识发光</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

> **后续改动**：**不再改动**

---

## 任务 6：创建 `.env.development`

> **文件作用**：环境变量文件。Vite 会自动读取 `VITE_` 开头的变量，注入到前端代码中。
> 代码里可以通过 `import.meta.env.VITE_API_BASE_URL` 读取。
> 类似后端的 `application.yml` 中可配置的参数。

**路径**：`源代码/zhiguang_fe/.env.development`

```env
# 开发环境 API 地址（Vite 开发服务器会通过 proxy 转发，所以留空即可）
VITE_API_BASE_URL=
```

> **后续改动**：
> - 生产环境需要创建 `.env.production`，填写真实后端地址
> - `VITE_API_BASE_URL` 在第二阶段 `apiClient.ts` 中被读取

---

## 任务 7：创建目录骨架

> **文件作用**：创建项目的目录结构。第一阶段只需要创建空目录，后续每个阶段往对应目录添加文件。

```bash
cd 源代码/zhiguang_fe/src

# 创建目录结构
mkdir -p components/layout
mkdir -p components/cards
mkdir -p components/common
mkdir -p components/icons
mkdir -p pages
mkdir -p services
mkdir -p types
mkdir -p context
mkdir -p theme
mkdir -p features/auth
```

> **后续说明**：
> - 第一阶段只需要创建目录，后续每个阶段往对应目录加文件
> - 最终完整结构见 `前端开发阶段.md`

---

## 任务 8：全局样式 `index.css`

> **文件作用**：全局 CSS 样式文件。定义颜色变量（CSS 自定义属性）、字体、背景渐变、布局网格（`.app-shell`）、工具类（`.ghost-button`）等。
> 类似后端的 `application.yml` 中的视觉配置，所有组件共用这些变量，保证整个应用风格一致。

**路径**：`源代码/zhiguang_fe/src/index.css`

```css
:root {
  font-family: "PingFang SC", "HarmonyOS", "SF Pro Display", "Helvetica Neue", Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;

  /* ===== 颜色系统 ===== */
  --color-primary: #ff8a41;
  --color-primary-strong: #ff7a22;
  --color-secondary: #ffe1bf;
  --color-accent: #ffcc73;
  --color-success: #12c07b;
  --color-info: #4b85f1;
  --color-text-strong: #1f1f33;
  --color-text: #3a3a4a;
  --color-text-muted: #76768a;
  --color-border: rgba(255, 163, 88, 0.25);
  --color-bg: #fff8ee;
  --color-surface: #ffffff;
  --color-surface-soft: #fff1de;

  /* ===== 圆角 ===== */
  --radius-sm: 10px;
  --radius-md: 16px;
  --radius-lg: 28px;

  /* ===== 阴影 ===== */
  --shadow-soft: 0 12px 32px rgba(255, 138, 65, 0.15);
  --shadow-card: 0 16px 40px rgba(26, 26, 44, 0.08);

  /* ===== 布局 ===== */
  --sidebar-width: 96px;

  background: var(--color-bg);
  color: var(--color-text);
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html,
body,
#root {
  margin: 0;
  width: 100%;
  min-height: 100%;
  background: radial-gradient(circle at top right, rgba(255, 188, 110, 0.2), transparent 45%),
    linear-gradient(180deg, #fffaf1 0%, #fff2de 100%);
}

body {
  margin: 0;
  display: flex;
  justify-content: center;
  background-attachment: fixed;
}

#root {
  width: 100%;
  display: flex;
  justify-content: center;
}

h1, h2, h3, h4, h5, h6 {
  margin: 0;
  font-weight: 600;
  color: var(--color-text-strong);
}

p { margin: 0; }

button, input, textarea, select {
  font: inherit;
}

a {
  color: inherit;
  text-decoration: none;
}

/* ===== 主布局：侧边栏 + 内容 ===== */
.app-shell {
  width: min(1400px, 100%);
  min-height: 100vh;
  display: grid;
  grid-template-columns: var(--sidebar-width) 1fr;
  gap: 24px;
  padding: 24px 32px 32px;
}

/* ===== 幽灵按钮（登录按钮用） ===== */
.ghost-button {
  padding: 10px 18px;
  border-radius: 999px;
  border: 1px solid rgba(255, 138, 65, 0.32);
  background: rgba(255, 255, 255, 0.76);
  color: var(--color-primary-strong);
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.ghost-button:hover {
  transform: translateY(-1px);
  box-shadow: 0 12px 24px rgba(255, 138, 65, 0.18);
}

/* ===== 响应式断点 ===== */
@media (max-width: 1200px) {
  :root { --sidebar-width: 88px; }
  .app-shell { padding: 16px 20px 24px; }
}

@media (max-width: 1024px) {
  .app-shell { grid-template-columns: 72px 1fr; }
}

@media (max-width: 900px) {
  .app-shell { grid-template-columns: 1fr; padding: 12px; }
}
```

> **后续改动**：
> - 第二阶段会在 `.ghost-button` 之后可能增加更多全局工具类
> - 第四阶段可能增加瀑布流相关的全局样式
> - 第六阶段会增加暗色模式支持

---

## 任务 8：Theme Tokens

> **文件作用**：TypeScript 版本的设计系统常量。将颜色、圆角、阴影等值导出为对象，供组件代码中引用。
> 和 `index.css` 中的 CSS 变量是"同一份数据，两种表达方式"。CSS 中用 `var(--color-primary)`，TS 中用 `colors.primary`。

**路径**：`源代码/zhiguang_fe/src/theme/tokens.ts`

```typescript
/**
 * 设计系统 Tokens。
 *
 * <p>与 index.css 中的 CSS 变量对应，供 TypeScript 代码中引用颜色、圆角等值。</p>
 */
export const colors = {
  primary: "#ff8a41",
  primaryStrong: "#ff7a22",
  primaryLight: "#ffe1bf",
  primarySoft: "#fff2e1",
  success: "#12c07b",
  info: "#4b85f1",
  surface: "#ffffff",
  surfaceSoft: "#fff6ea",
  surfaceMuted: "#fff0de",
  textStrong: "#1f1f33",
  text: "#3a3a4a",
  textMuted: "#76768a",
  border: "rgba(255, 163, 88, 0.3)",
  divider: "rgba(31, 31, 51, 0.08)"
};

export const radii = {
  xs: "8px",
  sm: "12px",
  md: "16px",
  lg: "24px",
  xl: "32px"
};

export const shadows = {
  card: "0 16px 42px rgba(26, 26, 44, 0.08)",
  soft: "0 14px 32px rgba(255, 138, 65, 0.18)"
};

export const layout = {
  sidebarWidth: 96,
  maxPageWidth: 1280,
  headerHeight: 88
};
```

**路径**：`源代码/zhiguang_fe/src/theme/index.ts`

```typescript
// 统一 re-export，外部 import { colors } from "@/theme" 即可
export * from "./tokens";
```

> **后续改动**：
> - 第一阶段完成后**不再改动**
> - 第六阶段如果增加暗色模式，会在此增加 `darkColors` 对象

---

## 任务 9：SVG 图标组件

> **文件作用**：SVG 图标的工厂函数。通过 `createIcon()` 统一创建风格一致的图标，所有图标共享 viewBox、strokeWidth 等样式属性。
> 类似后端的"模板方法模式"——先定义通用结构，再填充不同的 SVG path 数据。

**路径**：`源代码/zhiguang_fe/src/components/icons/Icon.tsx`

```typescript
import type { SVGProps } from "react";

export type IconProps = SVGProps<SVGSVGElement>;

/**
 * SVG 图标工厂函数。
 *
 * <p>所有图标共用统一的 viewBox、strokeWidth 和 strokeLinecap 设置。</p>
 */
const createIcon = (path: JSX.Element) => (props: IconProps) => (
  <svg
    width={props.width ?? 24}
    height={props.height ?? 24}
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth={1.6}
    strokeLinecap="round"
    strokeLinejoin="round"
    {...props}
  >
    {path}
  </svg>
);

/** 首页图标 */
export const HomeIcon = createIcon(
  <>
    <path d="M3.6 10.3 11.1 3.8a1.3 1.3 0 0 1 1.7 0l7.6 6.5" />
    <path d="M5.5 9.5V19a1 1 0 0 0 1 1h10.9a1 1 0 0 0 1-1v-9.4" />
    <path d="M9.5 20V13h5v7" />
  </>
);

/** 搜索图标 */
export const SearchIcon = createIcon(
  <>
    <circle cx="10.6" cy="10.6" r="5.8" />
    <path d="m16.2 16.2 4.2 4.2" />
  </>
);

/** 创作图标 */
export const CreateIcon = createIcon(
  <>
    <path d="M5.5 19v-5.1a2 2 0 0 1 2-2h3.3" />
    <path d="M5.5 19h4.6" />
    <path d="M13.6 5.4c.8-.8 2-.8 2.8 0l1.7 1.7c.8.8.8 2 0 2.8L12 15l-3.4.9.9-3.4z" />
  </>
);

/** 学习图标 */
export const StudyIcon = createIcon(
  <>
    <path d="M4 7.5 12 4l8 3.5-8 3.6z" />
    <path d="m6.8 16.4 5.2-2.2 5.2 2.2M20 7.6V16" />
    <path d="m4 7.6 0 8.4a2 2 0 0 0 1.2 1.8l5.6 2.4" />
  </>
);

/** 我的图标 */
export const ProfileIcon = createIcon(
  <>
    <circle cx="12" cy="8.3" r="3.2" />
    <path d="M6.2 19.6a6.6 6.6 0 0 1 11.6 0" />
  </>
);

/** Logo 星星图标 */
export const SparkIcon = createIcon(
  <>
    <path d="m12 2.8 1.5 4.6h4.9l-3.9 2.9 1.4 4.7L12 12.7 8.1 15l1.4-4.7-3.9-2.9h4.9z" />
  </>
);

/** 右箭头图标 */
export const ArrowRightIcon = createIcon(
  <>
    <path d="m10 7 5 5-5 5" />
    <path d="M5 12h10" />
  </>
);

/** 爱心图标（点赞用） — 第四阶段用 */
export const HeartIcon = createIcon(
  <>
    <path d="M12 19c-4.8-2.7-7.5-5.6-7.5-9a4.5 4.5 0 0 1 8.2-2.4h.6A4.5 4.5 0 0 1 19.5 10c0 3.4-2.7 6.3-7.5 9z" />
  </>
);

/** 书签图标（收藏用） — 第四阶段用 */
export const BookmarkIcon = createIcon(
  <>
    <path d="M7.5 4h9a1 1 0 0 1 1 1v14l-5.5-3.5L6.5 19V5a1 1 0 0 1 1-1z" />
  </>
);
```

> **后续改动**：
> - 第一阶段创建全部图标，后续**不再改动**
> - 如果需要新图标，在文件末尾追加 `createIcon(...)` 即可

---

## 任务 10：侧边栏组件

> **文件作用**：左侧导航栏组件。包含 Logo、5 个导航项（首页/搜索/创作/学习/我的）和底部品牌信息。
> 使用 `NavLink` 实现路由跳转，激活状态自动高亮。移动端自动变为底部横向导航。

**路径**：`源代码/zhiguang_fe/src/components/layout/Sidebar.tsx`

```typescript
import { NavLink } from "react-router-dom";
import { CreateIcon, HomeIcon, ProfileIcon, SearchIcon, SparkIcon, StudyIcon } from "@/components/icons/Icon";
import styles from "./Sidebar.module.css";

/**
 * 侧边栏导航。
 *
 * <p>桌面端垂直侧边栏，移动端自动变为底部横向导航栏。</p>
 */
const navItems = [
  { to: "/", label: "首页", Icon: HomeIcon },
  { to: "/search", label: "搜索", Icon: SearchIcon },
  { to: "/create", label: "创作", Icon: CreateIcon },
  { to: "/learn", label: "学习", Icon: StudyIcon },
  { to: "/profile", label: "我的", Icon: ProfileIcon }
] as const;

const Sidebar = () => {
  return (
    <aside className={styles.sidebar}>
      <div className={styles.logo}>
        <SparkIcon width={30} height={30} stroke="none" fill="#fff" />
      </div>
      <nav className={styles.nav}>
        {navItems.map(({ to, label, Icon }) => (
          <NavLink
            key={to}
            to={to}
            end={to === "/"}
            className={({ isActive }) => (isActive ? `${styles.link} ${styles.linkActive}` : styles.link)}
          >
            <Icon />
            {label}
          </NavLink>
        ))}
      </nav>
      <div className={styles.divider} />
      <div className={styles.footer}>
        <span>知光</span>
        <div>让知识发光</div>
      </div>
    </aside>
  );
};

export default Sidebar;
```

**路径**：`源代码/zhiguang_fe/src/components/layout/Sidebar.module.css`

```css
.sidebar {
  position: sticky;
  top: 24px;
  align-self: start;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 28px;
  padding: 28px 18px 24px;
  border-radius: 28px;
  background: rgba(255, 255, 255, 0.92);
  backdrop-filter: blur(20px);
  box-shadow: 0 18px 42px rgba(255, 152, 66, 0.14);
}

.logo {
  width: 52px;
  height: 52px;
  border-radius: 18px;
  background: linear-gradient(135deg, #ffaf73 0%, #ff822c 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  box-shadow: 0 12px 24px rgba(255, 138, 65, 0.36);
}

.nav {
  display: flex;
  flex-direction: column;
  gap: 12px;
  width: 100%;
}

.link {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 14px 4px 10px;
  color: var(--color-text-muted);
  font-size: 13px;
  font-weight: 500;
  border-radius: 18px;
  transition: all 0.22s ease;
}

.link svg {
  width: 28px;
  height: 28px;
}

.linkActive,
.link:hover {
  color: var(--color-primary-strong);
  background: rgba(255, 165, 88, 0.16);
  box-shadow: 0 12px 22px rgba(255, 138, 65, 0.16);
}

.divider {
  width: 100%;
  height: 1px;
  background: linear-gradient(90deg, rgba(255, 138, 65, 0.2), rgba(255, 138, 65, 0));
}

.footer {
  margin-top: 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  color: var(--color-text-muted);
  font-size: 12px;
  text-align: center;
}

.footer span {
  color: var(--color-primary-strong);
  font-weight: 600;
}

/* 移动端：侧边栏变底部导航 */
@media (max-width: 900px) {
  .sidebar {
    position: relative;
    top: 0;
    flex-direction: row;
    justify-content: space-between;
    width: 100%;
    padding: 18px 22px;
    border-radius: 22px;
  }

  .nav {
    flex-direction: row;
    justify-content: center;
  }

  .link {
    flex-direction: row;
    gap: 10px;
    font-size: 14px;
    padding: 10px 16px;
  }

  .link svg {
    width: 24px;
    height: 24px;
  }

  .divider,
  .footer {
    display: none;
  }
}
```

> **后续改动**：
> - `navItems` 在第二阶段可能会增加登录后才显示的项
> - CSS **基本不再改动**
> - 第二阶段 AuthContext 就绪后，Sidebar 可能根据登录状态显示不同内容

---

## 任务 11：AppLayout 布局组件

> **文件作用**：主布局壳子。组合 `Sidebar`（侧边栏）+ 内容区，通过 `variant` 参数决定内容区是否带白色卡片背景。
> 类似后端的 "模板 Controller"——定义通用布局结构，具体页面填入不同内容。

**路径**：`源代码/zhiguang_fe/src/components/layout/AppLayout.tsx`

```typescript
import type { ReactNode } from "react";
import Sidebar from "./Sidebar";
import styles from "./AppLayout.module.css";

type AppLayoutProps = {
  header?: ReactNode;
  children: ReactNode;
  variant?: "default" | "cardless";
};

/**
 * 主布局组件：侧边栏 + 内容区。
 *
 * <p>variant="default" 时内容区带白色卡片背景和阴影；
 * variant="cardless" 时不带卡片外壳（如登录/注册页用）。</p>
 */
const AppLayout = ({ header, children, variant = "default" }: AppLayoutProps) => {
  return (
    <div className="app-shell">
      <Sidebar />
      <div className={styles.container}>
        {header}
        <div className={variant === "default" ? styles.pageCard : styles.main}>{children}</div>
      </div>
    </div>
  );
};

export default AppLayout;
```

**路径**：`源代码/zhiguang_fe/src/components/layout/AppLayout.module.css`

```css
.container {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.content {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.main {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.pageCard {
  display: flex;
  flex-direction: column;
  gap: 24px;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 32px;
  padding: 32px;
  box-shadow: 0 20px 46px rgba(26, 26, 44, 0.06);
}

@media (max-width: 900px) {
  .container {
    gap: 16px;
  }

  .pageCard {
    border-radius: 24px;
    padding: 24px;
  }
}
```

> **后续改动**：
> - 第二阶段 AuthContext 就绪后，可能在 AppLayout 中增加登录检查逻辑
> - CSS **不再改动**

---

## 任务 12：路由定义 `App.tsx`

> **文件作用**：React Router 的路由定义。决定 URL 和页面组件的映射关系，类似后端的 `@RequestMapping`。
> 第一阶段只注册首页，后续阶段逐步放开其他路由。

**路径**：`源代码/zhiguang_fe/src/App.tsx`

```typescript
import { Navigate, Route, Routes } from "react-router-dom";
// 第一阶段只有这三个页面
// 后续阶段会逐步导入更多页面组件
// 第二阶段：导入 LoginPage, RegisterPage
// 第三阶段：导入 CreatePage, ProfilePage, EditProfilePage
// 第四阶段：导入 HomePage（完整版本）
// 第五阶段：导入 SearchPage, CourseDetailPage
// 第六阶段：导入 LearningPage
import HomePage from "./pages/HomePage";

function App() {
  return (
    <Routes>
      {/* 第一阶段：先放一个占位首页 */}
      <Route path="/" element={<HomePage />} />
      {/* 后续阶段会逐步放开这些路由 */}
      {/* <Route path="/search" element={<SearchPage />} /> */}
      {/* <Route path="/create" element={<CreatePage />} /> */}
      {/* <Route path="/learn" element={<LearningPage />} /> */}
      {/* <Route path="/profile" element={<ProfilePage />} /> */}
      {/* <Route path="/profile/edit" element={<EditProfilePage />} /> */}
      {/* <Route path="/post/:id" element={<CourseDetailPage />} /> */}
      {/* <Route path="/login" element={<LoginPage />} /> */}
      {/* <Route path="/register" element={<RegisterPage />} /> */}
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
  );
}

export default App;
```

> **后续改动**：
> - 第二阶段开始逐个取消注释路由
> - 第二阶段后可能增加路由守卫（`RequireAuth` 组件）

---

## 任务 13：入口文件 `main.tsx`

> **文件作用**：整个前端应用的启动入口。创建 React Root 节点，挂载 `BrowserRouter`（路由）和 `App`（路由定义）。
> 类似后端的 `ZhiGuangApplication.main()`，是程序的第一个被执行的代码。
> 第二阶段会在这里加上 `AuthProvider` 包裹，提供全局认证状态。

**路径**：`源代码/zhiguang_fe/src/main.tsx`

```typescript
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";
// 第二阶段取消下面这行的注释
// import { AuthProvider } from "./context/AuthContext";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <BrowserRouter>
      {/* 第二阶段加上 AuthProvider 包裹 */}
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

> **后续改动（第二阶段）**：
> - 将 `<App />` 包裹在 `<AuthProvider>` 中
> - 导入 `import { AuthProvider } from "./context/AuthContext";`

---

## 任务 14：首页占位

> **文件作用**：首页页面组件。第一阶段只显示占位文字，后续会接入真实 Feed 数据。

**路径**：`源代码/zhiguang_fe/src/pages/HomePage.tsx`

```typescript
import AppLayout from "@/components/layout/AppLayout";
import MainHeader from "@/components/layout/MainHeader";

/**
 * 首页 — 第一阶段占位。
 *
 * <p>第二阶段接入 AuthContext 显示登录状态；
 * 第四阶段接入 Feed API 显示知文列表。</p>
 */
const HomePage = () => {
  return (
    <AppLayout
      header={
        <MainHeader
          headline="知光 · 让思想有温度，让知识会发光"
          subtitle="欢迎来到知光平台"
        />
      }
    >
      <p style={{ color: "var(--color-text-muted)" }}>
        第一阶段：基础环境搭建完成。
      </p>
      <p style={{ color: "var(--color-text-muted)" }}>
        第二阶段将接入用户认证系统。
      </p>
    </AppLayout>
  );
};

export default HomePage;
```

> **后续改动**：
> - 第二阶段：加入 `AuthStatus` 组件到 header 的 rightSlot
> - 第四阶段：接入 `knowpostService.feed()` 拉取真实数据，用 `CourseCard` 渲染

---

## 任务 15：MainHeader 页面头部组件

> **文件作用**：页面头部组件。显示主标题、副标题，右侧可插入额外内容（如登录状态按钮）。
> 类似后端页面的"公共头部模板"，每个页面通过传入不同 props 复用。

**路径**：`源代码/zhiguang_fe/src/components/layout/MainHeader.tsx`

```typescript
import type { ReactNode } from "react";
import styles from "./MainHeader.module.css";

type MainHeaderProps = {
  headline: string;
  subtitle?: string;
  rightSlot?: ReactNode;
  children?: ReactNode;
};

/**
 * 页面头部：主标题 + 副标题 + 右侧插槽（通常放 AuthStatus）。
 *
 * <p>后续阶段可能增加 tabs 和 filters 插槽。</p>
 */
const MainHeader = ({ headline, subtitle, rightSlot, children }: MainHeaderProps) => {
  return (
    <div className={styles.header}>
      <div>
        <h1 className={styles.headline}>{headline}</h1>
        {subtitle && <p className={styles.subtitle}>{subtitle}</p>}
      </div>
      {rightSlot && <div>{rightSlot}</div>}
      {children}
    </div>
  );
};

export default MainHeader;
```

**路径**：`源代码/zhiguang_fe/src/components/layout/MainHeader.module.css`

```css
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.headline {
  font-size: 24px;
  font-weight: 700;
}

.subtitle {
  font-size: 14px;
  color: var(--color-text-muted);
  margin-top: 4px;
}

@media (max-width: 700px) {
  .headline { font-size: 20px; }
}
```

> **后续改动**：
> - 第四阶段可能增加 `tabs` 和 `filters` props
> - CSS **不再改动**

---

## 前端第一阶段完成检查

```bash
cd 源代码/zhiguang_fe

# 安装依赖（如果还没装）
npm install

# 启动开发服务器
npm run dev

# 浏览器打开 http://localhost:5173
# 应看到：
# - 左侧有导航栏（首页/搜索/创作/学习/我的）
# - 顶部有 "知光 · 让思想有温度，让知识会发光"
# - 页面内容显示 "第一阶段：基础环境搭建完成"
# - 暖橙色主题风格
```

> **预期结果**：
> - 侧边栏正常显示，点击首页高亮
> - 响应式布局：缩小窗口到 900px 以下，侧边栏变底部导航
> - 无控制台报错
>
> **注意事项**：
> - 后端不需要启动（前端第一阶段不依赖后端 API）
> - 确保 Vite proxy 配置正确（虽然第一阶段不调 API）

---

# 第一阶段完成标志

## 后端
- [x] `mvn compile` 成功
- [x] `mvn spring-boot:run` 启动成功
- [x] `http://localhost:8080/actuator/health` 返回 `{"status":"UP"}`
- [x] MySQL `zhiguang` 数据库和表创建完成
- [x] Redis 连接正常

## 前端
- [x] `npm run dev` 启动成功
- [x] `http://localhost:5173` 显示应用界面
- [x] 侧边栏导航正常显示
- [x] 暖橙色主题风格
- [x] 响应式布局生效（移动端侧边栏变底部导航）

---

# 下一步预告：第二阶段

完成第一阶段后，你将进入 **用户认证系统** 阶段：

**后端需要创建**：
- `user/domain/User.java` — 用户实体
- `user/mapper/UserMapper.java` — MyBatis Mapper
- `user/service/UserService.java` — 用户服务
- `auth/service/AuthService.java` — 认证服务
- `auth/token/JwtService.java` — JWT 令牌生成
- `auth/verification/VerificationService.java` — 验证码系统
- `auth/api/AuthController.java` — 认证接口
- `auth/config/PemUtils.java` — RSA 密钥读取
- `auth/config/AuthProperties.java` — JWT 配置绑定
- 各类 DTO（LoginRequest、RegisterRequest、TokenResponse 等）

**前端需要创建**：
- `src/context/AuthContext.tsx` — 认证全局状态
- `src/services/apiClient.ts` — 通用 fetch 封装
- `src/services/authService.ts` — 认证 API
- `src/types/auth.ts` — 认证类型定义
- `src/pages/LoginPage.tsx` — 登录页
- `src/pages/RegisterPage.tsx` — 注册页
- `src/features/auth/AuthStatus.tsx` — 登录状态展示
- `src/components/common/UserBadge.tsx` — 用户信息卡片
