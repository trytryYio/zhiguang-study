# 补课指南：把 zhiguang_new 补到阶段三完成

> 你的项目目前完成了约 20-25%，阶段四（布局）做得最好，但阶段二和三有硬伤。
> 本指南按优先级排列，你从第一步开始，一步步做。
> 参考完整版目录：`源代码/新修改/`

---

## 第一步：补阶段一的小问题（10分钟）

### 1.1 删掉无用的 App.css

你的项目根目录有个 `App.css`，这是 Vite 脚手架自动生成的模板 CSS，你的 `App.tsx` 根本没有 import 它。

**做法**：直接删除 `src/App.css`

**为什么**：死代码会误导人，以后你可能以为它有用而不敢删。

### 1.2 把 App.tsx 中的路由取消注释

当前你的 `App.tsx` 只有 `/` 路由是活的，其他 8 条全是注释。但路由本身不需要对应页面已写好——先把路由骨架搭出来，后面创建页面时就不需要回来改路由了。

**做法**：
1. 在 `App.tsx` 顶部，把所有被注释的 import 语句取消注释
2. 但那些页面文件还不存在，所以先创建空的占位页面（见下面模板）
3. 把所有 Route 取消注释

**占位页面模板**（每个页面都先这么写）：

```tsx
const XxxPage = () => {
  return <div>XxxPage 占位</div>;
};
export default XxxPage;
```

你需要创建这些占位文件：
- `src/pages/SearchPage.tsx`
- `src/pages/CreatePage.tsx`
- `src/pages/LearningPage.tsx`
- `src/pages/ProfilePage.tsx`
- `src/pages/EditProfilePage.tsx`
- `src/pages/CourseDetailPage.tsx`
- `src/pages/LoginPage.tsx`
- `src/pages/RegisterPage.tsx`

**验证**：`npm run dev`，浏览器访问 `/search`、`/create` 等路径，能看到占位文字即可。

---

## 第二步：补阶段二 — API 层（核心硬伤，约1小时）

### 2.1 理解你的 API 层现状

你的项目使用了 OpenAPI 自动生成的 API 客户端代码，具体表现为：
- `src/api/authController.ts` - 自动生成的认证相关 API 调用函数
- `src/api/typings.d.ts` - 自动生成的 TypeScript 类型定义
- `src/request.ts` - 你已经创建了这个文件，但需要确保它能被自动生成的代码正确调用

### 2.2 修复 request.ts 文件

你的 `request.ts` 文件已经存在，但有几个问题需要修复：

1. **函数签名不匹配**：自动生成的 `authController.ts` 调用 `request` 的方式是 `request<T>(url, { method, headers, data, params, ...options })`，但你的 `request` 函数签名是 `request<T>(path, method = "GET", body?: unknown)`。

2. **URL 拼接错误**：第 87 行有多余的 `}` 符号。

3. **响应处理**：你的响应拦截器已经处理了响应结构，但 `request` 函数内部又在处理，可能会导致重复处理。

**修复后的 request.ts 文件**：

```tsx
import axios, { type AxiosInstance, type AxiosRequestConfig } from 'axios';

// 获取 API 基础地址
const getBaseUrl = () => {
  const envBase = import.meta.env.VITE_BASE_URL as string | undefined;
  return envBase?.replace(/\/$/, "") ?? "";
};

// 从 localStorage 读取 accessToken
const getStoredToken = (): string | null => {
  try {
    const raw = localStorage.getItem('zhiguang_auth_tokens');
    if (!raw) return null;
    return (JSON.parse(raw) as { accessToken?: string }).accessToken ?? null;
  } catch {
    return null;
  }
};

// 创建 Axios 实例
const myAxios: AxiosInstance = axios.create({
  baseURL: getBaseUrl(),
  timeout: 60000,
  withCredentials: true,
});

// 请求拦截器 - 自动添加 token
myAxios.interceptors.request.use(
  function (config) {
    const token = getStoredToken();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  function (error) {
    return Promise.reject(error);
  }
);

// 响应拦截器 - 统一处理响应结构
myAxios.interceptors.response.use(
  function (response) {
    // 直接返回 data 中的数据，避免前端每次都写 .data.data
    return response.data.data;
  },
  function (error) {
    // 处理错误响应
    if (error.response?.data?.message) {
      console.error('请求错误:', error.response.data.message);
    }

    // 未登录处理
    if (error.response?.data?.code === 40100) {
      // 跳转到登录页
      if (!window.location.pathname.includes('/login')) {
        alert('请先登录'); // 或使用 message.warning
        window.location.href = `/login?redirect=${window.location.href}`;
      }
    }

    return Promise.reject(error);
  }
);

/**
 * 发送请求的通用函数
 * - 拼接完整 URL
 * - 自动从 localStorage 拿 Token，塞进 `Authorization: Bearer xxx`
 * - 发送 axios 请求
 * - 处理错误（非 2xx 抛异常）
 * - 统一处理后端响应结构，直接返回真正的数据
 */
async function request<T>(url: string, options: AxiosRequestConfig = {}): Promise<T> {
  // 构建完整的配置对象
  const config: AxiosRequestConfig = {
    url,
    method: options.method || 'GET',
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
    ...options,
  };

  // 处理查询参数
  if (options.params) {
    config.params = options.params;
  }

  // 处理请求体
  if (options.data) {
    config.data = options.data;
  }

  // 发送请求
  const response = await myAxios(config);
  return response as T;
}

export default request;
export { myAxios };
```

**验证**：创建完后，`authController.ts` 的 import 不再报错，并且能够正常调用。

### 2.3 关于 services 文件夹的说明

参考项目 `源代码/新修改` 中有 `services` 文件夹，里面有 `authService.ts` 等文件。这是一个**手动创建的服务层**，用于：

1. **解耦业务逻辑**：将 API 调用与业务逻辑分离
2. **类型转换**：将自动生成的 API 类型转换为业务层使用的类型
3. **统一错误处理**：在服务层处理 API 错误

**你是否需要创建 services 文件夹？**
- 如果你使用的是 OpenAPI 自动生成的代码，**不是必须的**，但建议创建，这样可以使代码结构更清晰
- 如果你直接手写 API 调用，那么 services 文件夹是一个很好的组织方式

**如何创建 authService.ts**：

1. 创建 `src/services` 文件夹
2. 创建 `src/services/authService.ts` 文件
3. 实现 authService 对象，调用自动生成的 authController 中的函数

---

## 第三步：补阶段三 — 认证系统（约2-3小时）

### 3.1 完成 AuthContext.tsx

你的 `AuthContext.tsx` 文件已经开始实现，但还未完成。以下是完整的实现：

```tsx
/**
 * 一个React认证上下文（Authentication Context），
 * 用于管理整个应用程序的用户认证状态。
 * 它是一个全局状态管理器，
 * 处理用户登录、注册、登出、令牌刷新等认证相关的所有逻辑。
 */

import { createContext, ReactNode, useCallback, useEffect, useMemo, useRef, useState } from "react";
import * as authController from "../api/authController";

// 定义认证令牌的类型
type AuthTokens = {
  accessToken: string; // 访问令牌，用于API认证
  refreshToken: string; // 刷新令牌，用于获取新的访问令牌
  expiresAt: number; // 访问令牌过期时间（毫秒时间戳）
};

// 定义认证上下文值的类型
type AuthContextValue = {
  user: API.AuthUserResponse | null; // 当前登录用户信息
  isLoading: boolean; // 是否正在加载认证状态
  tokens: AuthTokens | null; // 认证令牌
  login: (payload: API.LoginRequest) => Promise<void>; // 登录方法
  register: (payload: API.RegisterRequest) => Promise<API.AuthUserResponse>; // 注册方法
  logout: () => Promise<void>; // 登出方法
  refresh: () => Promise<void>; // 刷新令牌方法
  reloadUser: () => Promise<void>; // 重新加载用户信息方法
};

// 创建认证上下文
const AuthContext = createContext<AuthContextValue | undefined>(undefined);

// 定义本地存储的键名
const STORAGE_KEY = "zhiguang_auth_tokens";
const USER_STORAGE_KEY = "zhiguang_current_user";

// 从本地存储读取令牌
const readStoredTokens = (): AuthTokens | null => {
  // 检查是否在浏览器环境
  if (typeof window === "undefined") {
    return null;
  }
  // 从本地存储中获取令牌
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw) as AuthTokens; // 类型转换
    //如果令牌不存在 或者 令牌的备用令牌不存在 或者令牌已过期 则返回null
    if (!parsed.accessToken || !parsed.refreshToken || !parsed.expiresAt) {
      return null;
    }
    return parsed;
  } catch {
    return null;
  }
};

// 从本地存储读取用户信息
function readStoredUser(): API.AuthUserResponse | null {
  // 检查是否在浏览器环境
  if (typeof window === "undefined") {
    return null;
  }
  try {
    const raw = localStorage.getItem(USER_STORAGE_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw) as API.AuthUserResponse;
    // 检查用户对象 是否为空 或者 用户对象 不是object对象
    if (!parsed || typeof parsed !== "object") return null;
    return parsed;
  } catch {
    return null;
  }
}

// 将令牌持久化到本地存储
const persistTokens = (tokens: AuthTokens | null) => {
  // 检查是否在浏览器环境
  if (typeof window === "undefined") {
    return;
  }
  // 检查令牌对象 是否为空 ,是空的就在本地存储中删除
  if (!tokens) {
    localStorage.removeItem(STORAGE_KEY);
    return;
  }
  // 将令牌对象 转换为JSON字符串
  localStorage.setItem(STORAGE_KEY, JSON.stringify(tokens));
};

// 将用户信息持久化到本地存储
const persistUser = (user: API.AuthUserResponse | null) => {
  // 检查是否在浏览器环境
  if (typeof window === "undefined") {
    return;
  }
  if (!user) {
    localStorage.removeItem(USER_STORAGE_KEY);
    return;
  }
  try {
    localStorage.setItem(USER_STORAGE_KEY, JSON.stringify(user));
  } catch (e: any) {
    console.log(e.message);
  }
};

// 将字符串格式的时间转换为毫秒时间戳
const parseInstantToMillis = (value: string): number => {
  // 尝试当作数字处理
  const numeric = Number(value);
  if (!Number.isNaN(numeric)) {
    // 如果能成功转成数字，就按时间戳处理
    // 如果数值大于 1e12 则返回毫秒时间戳，否则认为是秒时间戳
    return numeric > 1e12 ? numeric : numeric * 1000;
  }
  // 如果不是数字，就走后面的日期字符串解析逻辑
  const t = Date.parse(value);
  // 如果解析失败，返回10分钟后的时间
  return Number.isNaN(t) ? Date.now() + 10 * 60 * 1000 : t;
};

// 将API响应的令牌格式转换为内部使用的令牌格式
const toTokens = (token: API.TokenResponse): AuthTokens => ({
  accessToken: token.accessToken,
  refreshToken: token.refreshToken,
  expiresAt: parseInstantToMillis(token.accessTokenExpiresAt),
});

// 定义AuthProvider的属性类型
type AuthProviderProps = {
  children: ReactNode; // 子组件
};

// 认证上下文提供者组件
export const AuthProvider = ({ children }: AuthProviderProps) => {
  // 状态管理
  const [tokens, setTokens] = useState<AuthTokens | null>(() => readStoredTokens());
  const [user, setUser] = useState<API.AuthUserResponse | null>(() => readStoredUser());
  const [isLoading, setIsLoading] = useState<boolean>(!!tokens);
  const fetchingRef = useRef<Promise<void> | null>(null);

  // 获取当前用户信息
  const fetchUser = useCallback(async (accessToken: string) => {
    try {
      // 调用API 获取当前用户信息
      const profile = await authController.me();
      setUser(profile); //更新用户状态
      persistUser(profile); //持久化用户信息
    } catch (error) {
      console.error("获取用户信息失败", error);
      setUser(null);
      setTokens(null);
      persistTokens(null);
      persistUser(null);
    }
  }, []);

  // 初始化时恢复登录态
  useEffect(() => {
    if (!tokens) {
      setIsLoading(false);
      return;
    }

    if (!fetchingRef.current) {
      const task = fetchUser(tokens.accessToken).finally(() => {
        fetchingRef.current = null;
        setIsLoading(false);
      });
      fetchingRef.current = task;
    }
  }, [tokens, fetchUser]);

  // 登录方法
  const login = useCallback(async (payload: API.LoginRequest) => {
    const response = await authController.login(payload);
    const nextTokens = toTokens(response.token);
    setTokens(nextTokens);
    persistTokens(nextTokens);
    setUser(response.user);
    persistUser(response.user);
    await fetchUser(nextTokens.accessToken);
  }, [fetchUser]);

  // 注册方法
  const register = useCallback(async (payload: API.RegisterRequest) => {
    const result = await authController.register(payload);
    // 注册成功后直接登录：写入令牌与用户信息
    const nextTokens = toTokens(result.token);
    setTokens(nextTokens);
    persistTokens(nextTokens);
    const userInfo = result.user;
    setUser(userInfo);
    persistUser(userInfo);
    // 为保证信息最新，拉取一次 /auth/me
    await fetchUser(nextTokens.accessToken);
    return userInfo;
  }, [fetchUser]);

  // 登出方法
  const logout = useCallback(async () => {
    if (tokens) {
      try {
        await authController.logout({ refreshToken: tokens.refreshToken });
      } catch (error) {
        console.warn("注销请求失败，继续清除本地状态", error);
      }
    }
    setTokens(null);
    setUser(null);
    persistTokens(null);
    persistUser(null);
  }, [tokens]);

  // 刷新令牌方法
  const refresh = useCallback(async () => {
    if (!tokens) return;
    try {
      if (Date.now() < tokens.expiresAt - 5000) {
        return;
      }
      const result = await authController.refresh({ refreshToken: tokens.refreshToken });
      const nextTokens = toTokens(result);
      setTokens(nextTokens);
      persistTokens(nextTokens);
      await fetchUser(nextTokens.accessToken);
    } catch (error) {
      console.error("刷新登录状态失败", error);
      await logout();
    }
  }, [tokens, fetchUser, logout]);

  // 重新加载用户信息方法
  const reloadUser = useCallback(async () => {
    if (!tokens) return;
    await fetchUser(tokens.accessToken);
  }, [tokens, fetchUser]);

  // 自动刷新令牌
  useEffect(() => {
    if (!tokens) {
      return;
    }
    const timer = window.setInterval(() => {
      void refresh();
    }, 60000);
    return () => window.clearInterval(timer);
  }, [tokens, refresh]);

  // 构建上下文值
  const value = useMemo<AuthContextValue>(
    () => ({
      user,
      tokens,
      isLoading,
      login,
      register,
      logout,
      refresh,
      reloadUser,
    }),
    [user, tokens, isLoading, login, register, logout, refresh, reloadUser]
  );

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
};

// useAuth hook
export const useAuth = () => {
  const context = React.useContext(AuthContext);
  if (!context) {
    throw new Error("useAuth 必须在 AuthProvider 内部使用");
  }
  return context;
};
```

### 3.2 修复 AuthStatus.tsx

你的 `AuthStatus.tsx` 文件有编译错误，以下是修复后的版本：

```tsx
import { useNavigate, useLocation } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';

const AuthStatus = () => {
  const { user, isLoading, logout } = useAuth();
  const navigate = useNavigate();
  const location = useLocation();

  const handleLogout = async () => {
    try {
      await logout();
      navigate('/');
    } catch (error) {
      console.error('退出失败', error);
    }
  };

  if (isLoading) {
    return <div>加载中...</div>;
  }

  if (!user) {
    return (
      <button
        onClick={() => {
          navigate('/login', {
            state: { from: location.pathname },
          });
        }}
      >
        登录
      </button>
    );
  }

  return (
    <div>
      <span>{user.username}</span>
      <button onClick={handleLogout}>退出</button>
    </div>
  );
};

export default AuthStatus;
```

### 3.3 在 main.tsx 中包裹 AuthProvider

修改 `src/main.tsx` 文件，用 `<AuthProvider>` 包裹 `<App />`：

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import { AuthProvider } from './context/AuthContext';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <AuthProvider>
        <App />
      </AuthProvider>
    </BrowserRouter>
  </React.StrictMode>
);
```

### 3.4 创建登录页和注册页

**登录页**：`src/pages/LoginPage.tsx`

```tsx
import { useState } from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import * as authController from '../api/authController';

const LoginPage = () => {
  const [phone, setPhone] = useState('');
  const [code, setCode] = useState('');
  const [countdown, setCountdown] = useState(0);
  const [isSending, setIsSending] = useState(false);
  const [error, setError] = useState('');

  const navigate = useNavigate();
  const location = useLocation();
  const { login } = useAuth();

  const from = (location.state as { from: string })?.from || '/';

  const handleSendCode = async () => {
    if (!phone) {
      setError('请输入手机号');
      return;
    }

    setIsSending(true);
    setError('');
    try {
      await authController.sendCode({
        scene: 'LOGIN',
        identifierType: 'PHONE',
        identifier: phone,
      });
      setCountdown(60);
      const timer = setInterval(() => {
        setCountdown((prev) => {
          if (prev <= 1) {
            clearInterval(timer);
            return 0;
          }
          return prev - 1;
        });
      }, 1000);
    } catch (err) {
      setError('发送验证码失败');
      console.error(err);
    } finally {
      setIsSending(false);
    }
  };

  const handleLogin = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!phone || !code) {
      setError('请输入手机号和验证码');
      return;
    }

    setError('');
    try {
      await login({
        identifierType: 'PHONE',
        identifier: phone,
        code,
      });
      navigate(from);
    } catch (err) {
      setError('登录失败，请检查验证码是否正确');
      console.error(err);
    }
  };

  return (
    <div>
      <h1>登录</h1>
      {error && <div style={{ color: 'red' }}>{error}</div>}
      <form onSubmit={handleLogin}>
        <div>
          <label>手机号：</label>
          <input
            type="tel"
            value={phone}
            onChange={(e) => setPhone(e.target.value)}
            placeholder="请输入手机号"
          />
        </div>
        <div>
          <label>验证码：</label>
          <input
            type="text"
            value={code}
            onChange={(e) => setCode(e.target.value)}
            placeholder="请输入验证码"
          />
          <button
            type="button"
            onClick={handleSendCode}
            disabled={isSending || countdown > 0}
          >
            {countdown > 0 ? `${countdown}秒后重发` : '发送验证码'}
          </button>
        </div>
        <button type="submit">登录</button>
      </form>
    </div>
  );
};

export default LoginPage;
```

**注册页**：`src/pages/RegisterPage.tsx`

```tsx
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import * as authController from '../api/authController';

const RegisterPage = () => {
  const [phone, setPhone] = useState('');
  const [code, setCode] = useState('');
  const [password, setPassword] = useState('');
  const [countdown, setCountdown] = useState(0);
  const [isSending, setIsSending] = useState(false);
  const [error, setError] = useState('');

  const navigate = useNavigate();
  const { register } = useAuth();

  const handleSendCode = async () => {
    if (!phone) {
      setError('请输入手机号');
      return;
    }

    setIsSending(true);
    setError('');
    try {
      await authController.sendCode({
        scene: 'REGISTER',
        identifierType: 'PHONE',
        identifier: phone,
      });
      setCountdown(60);
      const timer = setInterval(() => {
        setCountdown((prev) => {
          if (prev <= 1) {
            clearInterval(timer);
            return 0;
          }
          return prev - 1;
        });
      }, 1000);
    } catch (err) {
      setError('发送验证码失败');
      console.error(err);
    } finally {
      setIsSending(false);
    }
  };

  const handleRegister = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!phone || !code || !password) {
      setError('请输入手机号、验证码和密码');
      return;
    }

    setError('');
    try {
      await register({
        identifierType: 'PHONE',
        identifier: phone,
        code,
        password,
        agreeTerms: true,
      });
      navigate('/');
    } catch (err) {
      setError('注册失败，请检查信息是否正确');
      console.error(err);
    }
  };

  return (
    <div>
      <h1>注册</h1>
      {error && <div style={{ color: 'red' }}>{error}</div>}
      <form onSubmit={handleRegister}>
        <div>
          <label>手机号：</label>
          <input
            type="tel"
            value={phone}
            onChange={(e) => setPhone(e.target.value)}
            placeholder="请输入手机号"
          />
        </div>
        <div>
          <label>验证码：</label>
          <input
            type="text"
            value={code}
            onChange={(e) => setCode(e.target.value)}
            placeholder="请输入验证码"
          />
          <button
            type="button"
            onClick={handleSendCode}
            disabled={isSending || countdown > 0}
          >
            {countdown > 0 ? `${countdown}秒后重发` : '发送验证码'}
          </button>
        </div>
        <div>
          <label>密码：</label>
          <input
            type="password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            placeholder="请设置密码"
          />
        </div>
        <button type="submit">注册</button>
      </form>
    </div>
  );
};

export default RegisterPage;
```

### 3.5 关于 services 文件夹的补充说明

如果你想创建 services 文件夹，可以按照以下步骤：

1. 创建 `src/services` 文件夹
2. 创建 `src/services/authService.ts` 文件，内容如下：

```tsx
import * as authController from "../api/authController";

// 业务层类型定义
export type LoginRequest = {
  identifierType: "PHONE" | "EMAIL";
  identifier: string;
  code?: string;
  password?: string;
};

export type RegisterRequest = {
  identifierType: "PHONE" | "EMAIL";
  identifier: string;
  code: string;
  password: string;
  agreeTerms: boolean;
};

export type SendCodeRequest = {
  scene: "LOGIN" | "REGISTER";
  identifierType: "PHONE" | "EMAIL";
  identifier: string;
};

export type AuthenticatedUser = API.AuthUserResponse;
export type LoginResponse = API.AuthResponse;
export type RegisterResponse = API.AuthResponse;
export type SendCodeResponse = API.SendCodeResponse;
export type TokenResponse = API.TokenResponse;
export type RefreshResponse = API.TokenResponse;
export type LogoutRequest = {
  refreshToken: string;
};

export const authService = {
  sendCode: (payload: SendCodeRequest) =>
    authController.sendCode(payload) as Promise<SendCodeResponse>,

  register: (payload: RegisterRequest) =>
    authController.register(payload) as Promise<RegisterResponse>,

  login: (payload: LoginRequest) =>
    authController.login(payload) as Promise<LoginResponse>,

  logout: (payload: LogoutRequest) =>
    authController.logout(payload),

  fetchCurrentUser: () =>
    authController.me() as Promise<AuthenticatedUser>,

  refresh: (refreshToken: string) =>
    authController.refresh({ refreshToken }) as Promise<RefreshResponse>
};
```

3. 然后在 `AuthContext.tsx` 中使用 `authService` 替代直接调用 `authController`：

```tsx
// 导入 authService
import { authService } from "../services/authService";

// 在 fetchUser 中使用
const fetchUser = useCallback(async (accessToken: string) => {
  try {
    const profile = await authService.fetchCurrentUser();
    setUser(profile);
    persistUser(profile);
  } catch (error) {
    console.error("获取用户信息失败", error);
    setUser(null);
    setTokens(null);
    persistTokens(null);
    persistUser(null);
  }
}, []);

// 其他方法类似...
```

---

## 完成标志

当你做到以下所有事情，阶段一到三就补完了：

- [ ] `npm run dev` 无编译错误
- [ ] 所有路由能访问（至少显示占位文字）
- [ ] 点击顶栏"登录"→ 跳转登录页
- [ ] 登录页能发送验证码（需要后端）
- [ ] 登录后顶栏显示用户名+"退出"
- [ ] 点"退出"后回到未登录状态
- [ ] 刷新页面后登录态保持（localStorage 持久化）
- [ ] Token 过期前自动刷新

---

## 补完之后

阶段一到三补完后，你的项目和阶段四（布局导航）已经基本对齐了。接下来就可以跟着阶段五（通用组件）→ 阶段六（业务页面）→ 阶段七（AI功能）→ 阶段八（关系互动）往下走了。

每完成一个阶段，告诉我，我会出下一阶段的指引。

---

## 常见问题解答

### Q: 为什么参考项目中有 services 文件夹，而我的项目没有？

A: services 文件夹是一个手动创建的服务层，用于解耦业务逻辑和 API 调用。它不是 OpenAPI 自动生成的，而是开发者手动创建的。你可以根据需要决定是否创建这个文件夹。

### Q: 我可以通过后端注解让 OpenAPI 自动生成 services 文件夹吗？

A: 不可以。OpenAPI 只能生成 API 客户端代码（如 authController.ts），不能生成业务层的 services 文件夹。services 文件夹是前端开发者根据项目需要手动创建的。

### Q: 为什么我的 request.ts 文件需要修改？

A: 因为自动生成的 authController.ts 调用 request 函数的方式与你的实现不匹配。你需要确保 request 函数的签名与自动生成的代码的调用方式一致。

### Q: 为什么 AuthContext.tsx 中需要自动刷新 Token？

A: 为了提高用户体验，避免用户在使用过程中因为 Token 过期而需要重新登录。自动刷新 Token 可以在用户无感知的情况下保持登录状态。

### Q: 为什么登录和注册后需要再调用一次 /auth/me？

A: 因为后端返回的用户信息可能不完整，/auth/me 接口返回的是最权威的用户信息。这样可以确保前端获取到的用户信息是最新、最完整的。