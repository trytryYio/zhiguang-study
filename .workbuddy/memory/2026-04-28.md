# 2026-04-28

- 排查 zhiguang_new 头像上传失败：当前 openapi 生成的 uploadAvatar 已生成 FormData + requestType: "form"，真正问题在自定义 src/request.ts 默认强制设置 Content-Type: application/json，且未兼容 FormData / requestType=form，导致 multipart 请求被错误按 JSON 发送。源代码能正常上传的关键是其 request client 对 FormData 做了特判，不仅仅是 OpenAPI 文档正确。
- 分析源代码 relation 模块：后端以 /api/v1/relation 暴露 follow/unfollow/status/following/followers/counter 六类接口；服务层采用 Redis ZSet + DB 回填 + Outbox 异步事件维护关系列表，用户计数走 Redis SDS 固定结构并带采样校验/重建；前端通过 relationService + FollowButton + RelationCounters + RelationListModal 组合完成关注按钮、互关状态、计数展示和关注/粉丝列表分页弹窗。

