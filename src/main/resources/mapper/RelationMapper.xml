<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zhiguang.nauy.relation.mapper.RelationMapper">

    <select id="listFollowingRows" resultType="java.util.Map">
        SELECT to_user_id AS toUserId, created_at AS createdAt
        FROM following
        WHERE from_user_id=#{fromUserId} AND rel_status=1
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}

</select>
     <select id="listFollowerRows" resultType="java.util.Map">
         select from_user_id as fromUserId, created_at as createdAt
         from follower where to_user_id =#{toUserId} and rel_status=1
         ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset};
     </select>




</mapper>

